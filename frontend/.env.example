# Leave empty to use same-origin (/api) when frontend is served by backend.
VITE_API_BASE_URL=
# Used only for GitHub Pages redirect to the live server URL.
VITE_PUBLIC_APP_URL=http://144.91.96.77:8787
VITE_BASE_PATH=/
